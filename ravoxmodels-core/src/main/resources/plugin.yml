name: RavoxModels
main: com.ravox.models.core.RavoxModelsPlugin
version: ${project.version}
api-version: "1.20"
authors: [Ravox]
commands:
  ravoxmodels:
    description: RavoxModels admin and runtime commands
    usage: /ravoxmodels
    permission: ravoxmodels.admin
permissions:
  ravoxmodels.admin:
    description: Admin access to RavoxModels
    default: op
